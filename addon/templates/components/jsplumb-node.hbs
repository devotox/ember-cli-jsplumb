{{#re-sizable
  id=id
  onResize=(action "resizeNode")
  class=(concat "jsplumb-node " node.type)
  directions=(array "right" "bottom" "bottomRight")
}}
  {{#paper-card class="layout-column layout-align-center-center layout-fill"}}

    {{#if jsplumbUtils.editable}}
      {{#paper-content class="layout-row layout-align-space-between-center action-wrapper"}}
        {{paper-icon "edit" class="edit-node" click=(action "editNode")}}
        {{paper-icon "add_circle" class="connect-node"}}
        {{paper-icon "remove_circle" class="delete-node" click=(action "deleteNode")}}
      {{/paper-content}}

      {{#paper-content class="layout-column layout-align-center-center flex-100 label-wrapper"}}
        {{content-editable
          value=node.label
          tagName="span"
          allowNewlines=false
          placeholder="Enter Node Label"
        }}
      {{/paper-content}}

    {{else}}
      {{#paper-content class="layout-column layout-align-center-center flex-100 label-wrapper"}}
        {{node.label}}
      {{/paper-content}}
    {{/if}}

  {{/paper-card}}
{{/re-sizable}}